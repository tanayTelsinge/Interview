- Round 1 - Backend

- Which Java version used, list its features?
- 17, Records, switch expressions, instance of typed.

- class A has public void print(), class B overrides it which is called
 - B.

- What are Virtual Threads, diff between normal and virtual thread.

- What is completableFuture, difference from Future.

- ConcurrentHashMap vs HashMap

- Spring Cloud 
 - If u want centralized config, what to use?
 - Where u will setup auth.
 - If config repo changes, how updated changes get.
 
- Performance how to optimize if
 - API is slow. 
 - DB query is slow.

- LLM, RAG.
- What AI POC ChatBot did?
- Spring AI basics.

- Hibernate overview and annotations.
- Spring structure.

- Round 2 Frontend Angular

 - Which angular version used?
 - Its features?
 - Signals, useZonelessChangeDetection, @If, @detect

 - What are Reactive Forms?

 - What are operators in RxJS

 - What is ngRX?
   - what is Effect, reducer.
   - Can u use reducer to add, or only get.

- Do u know about observables explain.

- Performance optimizations for Angular app.

- What is Change Detection? OnPush etc.

- For Routing, how will u handle routing for two different modules.