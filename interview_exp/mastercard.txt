- Frontend
- What is observable? Can u write an observable example where u call api.

- If I tell u to create a dashboard, how will u approach it?

- Reactive vs Template Form, eg. write code where I want an input to only consider text not numbers.

- What is WCAG? which tags u used, google extension to check screen reading, what is wcag contrast value.

Backend
- what is hibernate merge and update.
- Did u work on spring security.
- AOP. where its used.
- Spring boot version.
- Is spring boot stateless. How to maintain state, and session.