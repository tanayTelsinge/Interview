# UML Examples

Place worked-out UML diagrams here (class diagrams, sequence diagrams, etc.)
for the LLD problems in `../../05_Problems/`.

Each problem already has its own `UML.md` next to its code — that is the canonical
location. Use this folder only for standalone reference diagrams not tied to a specific
problem.
